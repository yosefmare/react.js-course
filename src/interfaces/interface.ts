export interface MyPropsTitlePostsComponent {
    title: string,
    content: string,
    children?:React.ReactNode,
}

export interface SideMenuComponent {
    children?: React.ReactNode,
}
export interface MyPropsButtonsComponent {
    index?: number | string,
    word?: string,
}
